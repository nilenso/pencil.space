defmodule PencilSpaceServerWeb.APIController do
  @moduledoc """
  Generic API controller.
  """

  use PencilSpaceServerWeb, :controller
#  import PencilSpaceServerWeb.ControllerHelpers

#  def bad_params(conn),
#      do: render_json(conn, :bad_request, %{error: "Bad params for host"})
#
#  def not_found(conn, _params),
#      do: render_json(conn, :service_unavailable, %{error: "Route not found"})
end
